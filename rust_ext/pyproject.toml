[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "rust_ext"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "rust_ext"
features = ["pyo3/extension-module"]
